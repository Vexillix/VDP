# 图片字幕生成器 - 产品需求文档 (PRD)

## 1. 产品概述

**产品名称：** 图片字幕生成器 (Image Subtitle Generator)

**产品定位：** 一款用于在图片上添加多行字幕的工具，支持实时预览和自定义样式设置。

**核心价值：** 快速、便捷地为图片添加专业的多行字幕，每行字幕具有独立的背景块，呈现清晰的切割感。

## 2. 功能需求

### 2.1 图片上传功能

- **功能描述：** 用户可以选择并上传本地图片文件
- **交互方式：** 
  - 提供"选择图片"按钮（蓝色背景，白色加号图标）
  - 支持常见图片格式（JPG、JPEG、PNG等）
  - 上传后显示文件名（如：小红.jpeg）
- **技术要求：** 
  - 支持图片预览
  - 验证文件格式和大小

### 2.2 字幕样式配置

#### 2.2.1 字幕高度设置
- **功能：** 设置字幕区域的总高度
- **输入方式：** 数字输入框
- **单位：** px（像素）
- **默认值：** 80px
- **范围：** 建议 40-200px

#### 2.2.2 字体大小设置
- **功能：** 设置字幕文字的字体大小
- **输入方式：** 数字输入框
- **单位：** px（像素）
- **默认值：** 40px
- **范围：** 建议 12-100px

#### 2.2.3 字体颜色设置
- **功能：** 设置字幕文字的颜色
- **输入方式：** 颜色选择器 + 十六进制代码显示
- **默认值：** #FFFFFF（白色）
- **显示方式：** 颜色方块预览 + 十六进制代码输入框

#### 2.2.4 轮廓颜色设置
- **功能：** 设置字幕文字描边/轮廓的颜色
- **输入方式：** 颜色选择器 + 十六进制代码显示
- **默认值：** #000000（黑色）
- **显示方式：** 颜色方块预览 + 十六进制代码输入框

#### 2.2.5 字体样式设置
- **功能：** 选择字幕文字的字体族
- **输入方式：** 下拉选择框
- **选项：** 
  - 微软雅黑（默认）
  - 其他中文字体选项（宋体、黑体、楷体等）
- **技术要求：** 支持中文字体渲染

#### 2.2.6 字体粗细设置
- **功能：** 选择字幕文字的粗细程度
- **输入方式：** 下拉选择框
- **选项：** 
  - 正常（Normal，默认）
  - 粗体（Bold）
  - 细体（Light）
  - 其他粗细选项

### 2.3 字幕内容输入

- **功能描述：** 用户输入多行字幕文本
- **输入方式：** 多行文本输入框（Textarea）
- **特性：** 
  - 支持多行文本输入
  - 每行文本对应图片上的一行字幕
  - 支持中文输入
  - 文本换行对应字幕换行
- **示例内容：** 
  ```
  这是Manus的相关照片
  也是一个历史性的时刻
  Manus不一定会笑到最后
  但世界已经因此开始发生一些变化
  ```

### 2.4 实时预览功能

- **功能描述：** 在右侧预览区域实时显示添加字幕后的图片效果
- **显示内容：** 
  - 原始图片
  - 字幕叠加在图片底部
  - 字幕样式实时反映配置设置
- **布局要求：** 
  - 字幕位于图片底部
  - 字幕水平居中
  - 字幕区域高度根据配置显示
- **更新机制：** 配置或内容变化时实时更新预览

### 2.5 字幕背景切割效果（核心特性）

- **功能描述：** 每行字幕具有独立的背景块，呈现清晰的切割感
- **视觉效果：** 
  - 每行文字都有独立的背景矩形块
  - 行与行之间有明显的分割线或间距
  - 背景块颜色统一（通常为半透明或纯色）
  - 背景块宽度与图片宽度一致或根据文字宽度自适应
- **技术要求：** 
  - 每行字幕独立渲染背景
  - 背景块高度根据字体大小和行高计算
  - 支持背景透明度设置（可选）
  - 确保行与行之间视觉分离明显

### 2.6 生成字幕图片功能

- **功能描述：** 根据当前配置生成最终的带字幕图片
- **触发方式：** 点击"生成字幕图片"按钮（蓝色按钮）
- **处理流程：** 
  1. 读取原始图片
  2. 根据配置参数渲染字幕
  3. 将字幕叠加到图片底部
  4. 应用每行字幕的背景切割效果
  5. 生成最终图片
- **反馈机制：** 
  - 显示成功提示："字幕图片生成成功！"（绿色通知框，位于右下角）
  - 更新预览区域显示最终效果

### 2.7 保存图片功能

- **功能描述：** 将生成的带字幕图片保存到本地
- **触发方式：** 点击"保存图片"按钮（白色按钮）
- **保存格式：** 
  - 支持 PNG、JPG 等格式
  - 建议默认 PNG（保持透明度）
- **文件命名：** 可自动生成或使用原文件名+后缀

## 3. 界面布局要求

### 3.1 整体布局

- **布局方式：** 左右分栏布局
- **左侧面板：** 配置区域（宽度约 40-50%）
- **右侧面板：** 预览区域（宽度约 50-60%）
- **顶部：** 居中显示标题"图片字幕生成器"

### 3.2 左侧配置面板结构

1. **上传图片区域**
   - 上传按钮
   - 文件名显示

2. **字幕设置区域**
   - 标题："字幕设置"
   - 配置项列表（垂直排列）
   - 每个配置项包含：标签 + 输入控件 + 单位（如适用）

3. **字幕内容区域**
   - 标题："字幕内容"
   - 多行文本输入框

4. **操作按钮区域**
   - "生成字幕图片"按钮（主要操作，蓝色）
   - "保存图片"按钮（次要操作，白色/灰色）

### 3.3 右侧预览面板

- **标题：** "预览"
- **预览区域：** 
  - 显示图片和字幕的合成效果
  - 保持图片原始宽高比
  - 字幕叠加在底部

### 3.4 通知提示

- **位置：** 右下角
- **样式：** 绿色矩形框
- **内容：** 操作成功提示信息
- **显示时长：** 3-5秒后自动消失

## 4. 技术实现要点

### 4.1 前端技术栈建议

- **框架：** React / Vue.js / 原生 JavaScript
- **图片处理：** Canvas API 或 HTML5 Canvas
- **颜色选择器：** 使用现成的颜色选择器组件
- **文件上传：** FileReader API 或 FormData

### 4.2 核心实现逻辑

1. **图片加载与预览**
   - 使用 FileReader 读取本地文件
   - 创建 Image 对象加载图片
   - 在 Canvas 上绘制图片

2. **字幕渲染**
   - 使用 Canvas 的 fillText 和 strokeText 绘制文字
   - 应用字体样式、大小、颜色、轮廓颜色
   - 计算文字位置（底部居中）

3. **背景切割效果实现**
   - 为每行文字计算独立的位置和尺寸
   - 使用 fillRect 为每行绘制背景矩形
   - 设置背景颜色和透明度
   - 确保行与行之间有间距或分割线

4. **图片生成**
   - 将 Canvas 内容转换为图片（toDataURL）
   - 支持指定输出格式和质量

5. **图片下载**
   - 创建下载链接（<a> 标签）
   - 触发下载事件

### 4.3 样式计算

- **字幕区域高度：** 根据配置的"字幕高度"设置
- **每行高度：** 根据字体大小和行高计算
- **行数：** 根据输入文本的换行符数量确定
- **背景块高度：** 每行高度 = 字幕区域总高度 / 行数（或根据字体大小动态计算）
- **文字位置：** 垂直位置 = 图片高度 - 字幕区域高度 + 行索引 * 行高

## 5. 用户体验要求

### 5.1 交互体验

- **实时预览：** 配置修改后立即更新预览
- **响应速度：** 预览更新应在 100ms 内完成
- **操作反馈：** 所有操作都有明确的视觉反馈
- **错误处理：** 图片格式错误、文件过大等情况给出友好提示

### 5.2 视觉体验

- **界面风格：** 简洁、现代、白色背景
- **按钮样式：** 
  - 主要操作：蓝色背景，白色文字
  - 次要操作：白色背景，深色文字，边框
- **输入框样式：** 清晰的边框，合适的间距
- **预览区域：** 清晰的边框或阴影，突出预览效果

## 6. 边界情况处理

### 6.1 图片相关

- **超大图片：** 限制上传图片大小或进行压缩
- **图片格式：** 仅支持常见图片格式，其他格式给出提示
- **图片尺寸：** 超大图片需要缩放预览，但保持原始比例

### 6.2 字幕相关

- **空内容：** 字幕内容为空时，预览区域不显示字幕
- **超长文本：** 单行文本过长时，考虑自动换行或截断
- **行数过多：** 字幕行数过多时，确保字幕区域高度足够或提供滚动

### 6.3 配置相关

- **无效输入：** 数字输入框需要验证，防止负数或非数字
- **颜色值：** 验证十六进制颜色值格式
- **字体加载：** 确保所选字体可用，否则使用默认字体

## 7. 未来扩展功能（可选）

- 字幕位置选择（顶部、底部、居中）
- 字幕背景透明度设置
- 字幕对齐方式（左对齐、居中、右对齐）
- 字幕动画效果
- 批量处理多张图片
- 预设样式模板
- 字幕阴影效果
- 多语言支持

## 8. 验收标准

1. ✅ 能够成功上传并预览图片
2. ✅ 所有样式配置项都能正常工作并实时更新预览
3. ✅ 字幕内容能够正确显示在图片底部
4. ✅ **每行字幕具有独立的背景块，呈现清晰的切割感**（核心验收点）
5. ✅ 生成的字幕图片质量清晰，文字可读
6. ✅ 能够成功保存生成的图片到本地
7. ✅ 操作反馈及时、友好
8. ✅ 界面布局与设计稿一致

---

**文档版本：** v1.0  
**创建日期：** 2024  
**最后更新：** 2024

